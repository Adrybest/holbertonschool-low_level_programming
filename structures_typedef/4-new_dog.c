#include "dog.h"
/**
 * new_dog - creates a new dog
 * strlen - calcule la longueur d'une cha√Æne donn√
 * @ptr: ptr
 * @name: char
 * @age: float
 * @owner: char
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	int strlen(char *ptr)
	{
		int a;

		for (a = 0; ptr[a]; a++)
		{
		}
		return(ptr)
	}
}
